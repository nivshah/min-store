_ms = {
  // this code is going to allow users to write code like:
  //   _ms.push(['operation','key','value'])
  // where 'value' is optional and operation is one of the following:
  //   read, create, delete, increment, decrement
}